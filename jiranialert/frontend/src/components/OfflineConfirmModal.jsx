import { AnimatePresence, motion } from 'framer-motion'
import { Circle, X } from 'lucide-react'

export default function OfflineConfirmModal({ open, onCancel, onConfirm, busy = false }) {
  return <AnimatePresence>{open ? <motion.div className="fixed inset-0 z-[120] flex items-center justify-center bg-slate-950/55 px-4 backdrop-blur-sm" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) onCancel() }}><motion.section initial={{ opacity: 0, y: 18, scale: 0.96 }} animate={{ opacity: 1, y: 0, scale: 1 }} exit={{ opacity: 0, y: 12, scale: 0.97 }} className="relative w-full max-w-md rounded-2xl border border-slate-200 bg-white p-6 shadow-2xl" role="dialog" aria-modal="true" aria-labelledby="offline-title"><button type="button" onClick={onCancel} disabled={busy} className="absolute right-4 top-4 rounded-lg p-2 text-slate-400 hover:bg-slate-100" aria-label="Close offline confirmation"><X className="h-5 w-5" /></button><div className="flex h-12 w-12 items-center justify-center rounded-xl bg-amber-100 text-amber-700"><Circle className="h-6 w-6 fill-current" /></div><h2 id="offline-title" className="mt-5 text-2xl font-black text-slate-900">Go offline?</h2><p className="mt-2 text-sm leading-6 text-slate-600">You will stop showing as available for new responder assignments. You can return online at any time.</p><div className="mt-6 flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><button type="button" onClick={onCancel} disabled={busy} className="rounded-xl border border-slate-300 bg-white px-5 py-3 text-sm font-bold text-slate-700 hover:bg-slate-50">Stay Online</button><button type="button" onClick={onConfirm} disabled={busy} className="rounded-xl bg-amber-600 px-5 py-3 text-sm font-bold text-white hover:bg-amber-700 disabled:opacity-60">{busy ? 'Updating...' : 'Go Offline'}</button></div></motion.section></motion.div> : null}</AnimatePresence>
}
