{
  "name": "functions",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "node --check index.js",
    "serve": "node ../scripts/kill-stale-emulators.js && firebase emulators:start --config ../firebase.json --project jiranialert --only functions,firestore,auth",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^7.2.5",
    "nodemailer": "^8.0.7"
  }
}
